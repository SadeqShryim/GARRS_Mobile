# SDD ledger — plan: docs/superpowers/plans/2026-09-05-slice1-foundation-garage.md

Spec: docs/superpowers/specs/2026-09-05-slice1-foundation-garage-design.md (read; binding authority)

## Session rulings (from the user, 2026-09-05)
- Ruling: git is on hold by the user — no worktree, no branch, no commits. Each task's checkpoint = `npm test` + `npm run typecheck` passing; the controller re-runs both after each report to confirm. — why: explicit user instruction — cost if wrong: no undo between tasks until git is initialised; mitigated by the ledger and by asking the user to lift the hold as soon as they are willing.
- Ruling: per-task reviewer dispatches are waived — the user said "no need to do any review since all you're doing is making the design I've already created". Verification (tests, typecheck, emulator screenshots vs. references) still runs. — why: user instruction — cost if wrong: a spec drift that tests don't catch reaches Task 22's visual pass instead of being caught earlier.
- Ruling: no Playwright npm/Chromium download — reference shots use the Playwright MCP attached to the session (fallback: Chrome extension, live), asset baking is pure Node (`pngjs`). Plan Tasks 1, 3, 4 updated accordingly. — why: user declined the download — cost if wrong: if the MCP cannot launch a browser, references are live-only and `verification.md` records that.
- Approved by the user: Expo Go install onto the emulator; persisting ANDROID_HOME + PATH as user env vars.

## Pre-flight scan (plan vs. itself and Global Constraints)

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T5 → T10–T21 | `color/font/ease/dur/blur/layout/bez` | every key used later exists in T5 (ink2–5, ink7, hair07/13/14, tint02/05, sunken2, input, disabled, disabledInk, handle, shellDark, splash, textOnDark, blueDeep, amberBright; dur.dim/fade/sheet/screen/hump/color/gauge/metalIdle/metalPressed/ripple/blob/press; ease.cssEaseOut) — OK |
| T6 → T7, T9, T13, T16–T20 | `Vehicle`, `TabId`, `SEED_VEHICLES`, `DECODE`, `DEMO_VIN`, `TABS(TabDef)`, `VIN_SPOTS`, `RECALL_ROWS` | names match at every use — OK |
| T7 → T9, T16, T17 | `decodeVin`, `maskVin`, `vinOk`, `fleetLine`, `counter`, `recallCount`, `isOpenRecall`, `statsFor(v, scheduled)` with `service[].pct: number` | T17 renders `${m.pct}%`; T7 tests assert numbers — OK |
| T8 → T16 | `CARD_W, GAP, STEP, railPadding, slotForOffset, liveIndex, middleSlot, recenterSlot` | all imported by T16 Rail — OK |
| T9 → T13, T14, T16–T21 | store fields + actions; `openVinHelp` leaves `sheet` intact | T18/T19 tests depend on that — consistent — OK |
| T10 → T11–T21 | `Icon(name: IconName|string)`, `Sans/Mono`, `useAppBlurTarget`, root layout comment placeholder | T14 replaces the placeholder comment with `<OverlayHost />` — OK |
| T4 → T11, T12, T21 | PNG sizes/constants (768 / 210 / 48; 1536² and 612²) | T5 `layout.bezelBaked/blobBaked/blobBleed` match; pure-Node bake yields (210+96)·2 = 612 — OK |
| T12 → T16, T17 | `GlowCard` prop `faceStyle` (spec said `facePadding`) | one name used everywhere in the plan — OK (spec wording differs, same intent) |
| T13 → T16 | `app/(tabs)/garage/index.tsx` placeholder | T16 Step 7 replaces the body — OK |
| T14 → T18–T21 | `OverlayHost` grows one line per task; final order add → recall → vinhelp → Toast → Splash | listed in T21 Step 4 — OK |
| T15 → T17 | `HealthGauge({health, gaugeColor, word})` | matches — OK |
| T16 → T17 | route push to `/(tabs)/garage/[id]` before T17 creates it | plan text acknowledges a possible typed-routes error until T17 — accepted |
| T13 own text | `navigation.navigate(t.id, …)` typing | plan carries the `as never` fallback — OK |
| T1 own text | jest.setup mocks vs. libraries used in tests (safe-area, expo-font, vector-icons, expo-blur incl. BlurTargetView ref, masked-view, linear-gradient) | covers every native module the components import — OK |
| Global: "always pass blurTarget" | Sheet uses `useAppBlurTarget()`; OverlayHost sits inside the provider in T10's layout | OK |
| Global: no RN `<Modal>` | none used anywhere — OK |

Scan result: no conflicts requiring a ruling beyond the session rulings above.

## Progress
- Task 1: dispatched (sonnet) — brief task-1-brief.md, report task-1-report.md
- Task 2 Step 1 (ANDROID_HOME + PATH user env vars): done by controller (environment, not code) while Task 1 ran
- Task 3 pre-check: Playwright MCP launched a browser (about:blank) with no download → primary path confirmed. It writes snapshots to `<cwd>/.playwright-mcp/` — add to .gitignore when git starts.
- Task 2 Step 2 (AVD `s24proxy`): created by the user in Android Studio on 2026-09-05 (Pixel 8, API 35 Google Play x86_64, 1080×2400 @420dpi). Verify with `"$LOCALAPPDATA/Android/Sdk/emulator/emulator.exe" -list-avds`.
- All task briefs 1–23 are extracted to this directory as `task-N-brief.md`; dispatch reads the brief, never the plan.

- Task 1: complete (no commits — git hold; controller re-ran `npm test` 2/2 + `npm run typecheck` clean). Implementer's environment findings that LATER TASKS must respect: `tsconfig.json` has `"types": ["jest"]`; `@types/jest` pinned to 29 (jest 29.7); `@expo/vector-icons` installed explicitly; `app.json` has no `newArchEnabled`/`edgeToEdgeEnabled` (not in SDK 57 schema — both are always-on); jest.setup.ts carries the worklets mock at `react-native-worklets/lib/module/mock`; template example dirs were under `src/` and were removed; `create-expo-app` was run with `--no-agents-md` so CLAUDE.md survived. Report: task-1-report.md.
- Task 2: dispatched (sonnet) for Steps 3–6 only.
- Ruling: Tasks 2, 3 and 4 run in parallel — Task 2 writes only `docs/reference/emu-smoke.png`, Task 3 writes `scripts/serve-design.mjs` + `docs/reference/<state>.png`, Task 4 writes `scripts/bake-assets.mjs`, `scripts/build-remix-glyphmap.mjs`, `src/assets/**`; no shared files, and each uses a different resource (emulator / Playwright MCP browser / Node). — why: user is near a session limit; the skill's "never parallel" rule exists to prevent file conflicts, which cannot occur here — cost if wrong: two agents editing the same file; mitigated by the disjoint file lists in each dispatch. Tasks 5+ go back to strictly sequential.
- Task 3: dispatched (sonnet). Task 4: dispatched (sonnet).
- Ruling extended: Tasks 5, 6, 8 also run in parallel (files: `src/theme/tokens.ts` + `src/lib/gradient.ts`; `src/fixtures/*`; `src/lib/rail.ts` — pairwise disjoint, no cross-imports). Task 7 waits for 6 (imports fixtures); Task 9 waits for 7. Model: haiku for 5/6/8 (complete code in brief — transcription + tests).
- Tasks 5, 6, 8: dispatched (haiku).
- Task 2: BLOCKED — host is **ARM64 Windows (Snapdragon X)**; the x86_64 API 35 image cannot run ("x86_64 emulation currently requires hardware acceleration … Virtualization extension is not supported"). Only the x86_64 image is on disk. Options put to the user: (A) recreate `s24proxy` on `system-images;android-35;google_apis_playstore;arm64-v8a` (multi-GB download, needs Windows Hypervisor Platform), or (B) use the S24 Ultra itself over USB/adb as the verification device (no download; it IS the demo device). Report: task-2-report.md. Spec §12.1 and every "verify on the emulator" step are affected by the choice.
- User decision (2026-09-05): **download the ARM64 image (~2 GB) and recreate `s24proxy` on arm64-v8a** — approved download. Spec §12.1, plan Task 2 Step 2, and CLAUDE.md updated; task-2-brief.md must be regenerated from the plan before re-dispatch. Task 2 re-dispatch (Steps 3–6) waits for the user to finish the AVD.
- Reports received: Task 3 DONE_WITH_CONCERNS (11 refs at 430×932; toast held open by patching setTimeout for capture — render identical; later refs show 4–6 vehicles from retakes, layout still valid); Task 4 DONE_WITH_CONCERNS (added `"node"` to tsconfig `types` — Ruling: accepted, additive and required for the node-based test); Task 5 DONE; Task 6 DONE_WITH_CONCERNS (only transient typecheck noise from concurrent work); Task 8 DONE_WITH_CONCERNS (same transient noise). Controller verification of the merged tree follows.
- Controller verification (merged tree after 3/4/5/6/8): `npm test` 5 suites / 57 tests pass; `npm run typecheck` clean; 11 reference PNGs in docs/reference; 4 PNGs + TTF + glyph map in src/assets.
- Task 3: complete (controller-verified). Task 4: complete (controller-verified). Task 5: complete (controller-verified). Task 8: complete (controller-verified).
- Task 6: tests + types verified; punctuation byte-check FOUND A DEFECT — `vinHelp.ts` has straight apostrophes (U+0027) in the three `driver’s` strings instead of U+2019 (em-dash and middle dots correct). Spec-compliance finding (fidelity mandate: copy verbatim). Fix round 1/5 dispatched as a fresh haiku agent with the exact Node one-liner (SendMessage to the original agent would have cost a tool-load round-trip; the fix is 3 characters).
- Task 6: fix round 1/5 (1 addressed, 0 open — apostrophes now U+2019 ×3; fixtures 4/4, typecheck clean).
- Task 6: complete (controller-verified after the fix).
- Task 7: complete (controller-verified: 13/13 derive; a transient typecheck failure during the check was Task 10 mid-write — re-run clean seconds later).
- Task 9: dispatched (haiku), parallel with Task 10 (disjoint: `src/store/`).
- Task 9: DONE per report (6/6 store tests; a DotPattern test failure it saw belongs to Task 10 in flight) → controller check → complete.
- Task 12: dispatched (sonnet), parallel with 9 and 10 (disjoint: `src/ui/GlowCard.tsx` + test; depends only on tokens + glow-blob.png). Tasks 11 and 15 wait for Task 10 (they import `Icon`/`Txt`).
- Task 12: complete (controller-verified 2/2). DotPattern test now passes (Task 10 resolved it in flight).
- Task 10: DONE_WITH_CONCERNS (81/81, typecheck clean). Concern = it fixed the `@react-native-masked-view/masked-view` mock in `jest.setup.ts` to forward `testID`/props — Ruling: accepted; the original mock (written in the plan's Task 1) dropped props and made the brief's own DotPattern test unpassable — cost if wrong: none, additive. Step 9 (emulator) deferred → covered by Task 2 re-run. Task 10: complete (controller-verified: Txt + DotPattern 3/3, typecheck 0 errors).
- Task 15: complete (controller-verified 2/2, typecheck clean).
- Task 11: complete (controller-verified 2/2; the only typecheck error at check time was Task 14's in-flight `Toast.test.tsx` before `Toast.tsx` existed — transient).
- Task 14: DONE per report (92/92, typecheck clean). Shared-file change: fixed the `react-native-safe-area-context` mock in `jest.setup.ts` to `require(...).default` — Ruling: accepted; the plan's original line never unwrapped the default export so `useSafeAreaInsets` was undefined in tests — cost if wrong: none, it matches the package's declarations. → controller check → complete.
- Tasks 18, 19, 20, 21: dispatched in parallel (18/20 sonnet, 19/21 haiku) with the instruction NOT to edit `src/overlays/OverlayHost.tsx`; one batch mount dispatch follows. Task 16 waits for Task 13 (its Step 7 edits `app/(tabs)/garage/index.tsx`).
- Plan for Tasks 18–21 (all four edit `src/overlays/OverlayHost.tsx` by one line each): dispatch the four screens in parallel with the instruction NOT to touch OverlayHost, then ONE small batch dispatch mounts all four in the final z-order (add → recall → vinhelp → Toast → Splash). Their tests render the screens directly, so nothing is lost by deferring the mount.
- Tasks 13 and 14: dispatched (sonnet) in parallel — 13 writes `src/ui/HumpTabBar.tsx`, `src/screens/TabStub.tsx`, `app/(tabs)/**`, deletes `app/index.tsx`; 14 writes `src/ui/{Sheet,SlideUpScreen,Toast,OutlinePill}.tsx`, `src/overlays/OverlayHost.tsx`, and edits `app/_layout.tsx` — disjoint.
- Tasks 11 and 15: dispatched (sonnet) in parallel — they import `Icon`/`Txt` (present, passing) and write only `src/ui/MetalButton.tsx` / `src/ui/HealthGauge.tsx` + tests. Both emulator steps deferred. Tasks 13 and 14 wait for Task 10's report (they edit `app/`).
- Task 7: dispatched (haiku).
- Task 10: dispatched (sonnet) in parallel with Task 7 and the Task 6 fix — disjoint files (`app/`, `src/ui/{Icon,Txt,DotPattern}.tsx`, `src/overlays/blurTarget.tsx`, their tests). Its Step 9 (emulator check) is DEFERRED to the controller until the ARM64 `s24proxy` exists; the Task 2 re-run's smoke screenshot will show this task's fonts screen and serves as that check.

## RESUME HERE (rewritten 2026-09-05, session 3 — read this block first)
CURRENT STATE: ALL CODE TASKS COMPLETE — Tasks 1, 3–21 plus the overlay mount, each controller-verified; merged tree = `npm test` 22 suites / 113 tests, `npm run typecheck` clean. A `task-N-report.md` exists for 1–21 plus `task-mount-report.md`. Nothing is in flight; the previous session ended cleanly after the user asked for the docs to be updated before a computer update.
REMAINING (all hardware-bound, in order):
1. Task 2 Steps 3–6 (boot `s24proxy`, `npm run android` installs Expo Go [approved], smoke screenshot → `docs/reference/emu-smoke.png`). BLOCKED until the user recreates AVD `s24proxy` (Pixel 8) on `system-images;android-35;google_apis_playstore;arm64-v8a` in Android Studio (no cmdline-tools, so it cannot be scripted). Check before dispatch: `grep abi.type "$USERPROFILE/.android/avd/s24proxy.avd/config.ini"` must say `arm64-v8a` (it still said x86_64 at the last check). Dispatch from `task-2-brief.md` (already regenerated for arm64). Alternative the user may choose: the S24 Ultra over USB (`adb devices` lists it) as the verification device.
2. Task 22 (emulator fidelity pass vs. `docs/reference/*.png`, final blur values in `src/theme/tokens.ts`, write `docs/reference/verification.md`) — needs 2. Also absorbs the deferred emulator checks of Tasks 10, 11, 15, 16, 17 and the re-check of typed-route hrefs once `expo start` has generated `.expo/types`.
3. Task 23 (on-device pass on the S24 Ultra, append to verification.md) — needs the phone on Wi-Fi/USB with USB debugging.
Rules still in force: no git (no repository exists), no reviewer dispatches, no Playwright/Chromium download, gate after each task = `npm test && npm run typecheck`. Model plan: sonnet for 2/22/23.

HISTORY (superseded resume notes, kept for the record):
UPDATE 2: COMPLETE + controller-verified: Tasks 1, 3, 4, 5, 6 (after 1 fix round), 7, 8, 9, 12. BLOCKED: Task 2 — waiting for the user to recreate AVD `s24proxy` on the **arm64-v8a** API 35 Google Play image (this PC is ARM64); when it exists, re-dispatch Task 2 Steps 3–6 from the regenerated `task-2-brief.md`, and its smoke screenshot also covers the deferred emulator checks of Tasks 10/11. IN FLIGHT at this write: Tasks 10, 11, 15 (reports pending — a `task-N-report.md` means finished). NEXT: 13 and 14 after 10 (they edit `app/`); 16 after 11+12+13+14; 17 after 15+16; then 18 → 23.
UPDATE 1: Task 1 is COMPLETE (verified). At that write, **Tasks 2, 3, 4, 5, 6, 8 were all running in parallel** (see the rulings above) with no reports yet. To resume: for each of those, a `task-N-report.md` in this directory means it finished — read its Status line; no report means it was cut off — check its files (listed in the parallel rulings) and re-dispatch only what's missing. Then Task 7 (needs 6), Task 9 (needs 7), Task 10 (needs 2, 4, 5), and 11 → 23 sequentially. After every task the controller runs `npm test && npm run typecheck` and appends `Task N: complete` here.
Original note — state at the time of writing: **Task 1 implementer was still running in the background** (sonnet, no report yet). Observed on disk at that moment: `package.json`, `app.json`, `jest.setup.ts`, `node_modules/`, `src/` all present; `task-1-report.md` not yet written — i.e. scaffold + install were done and the implementer was in the test/typecheck/report phase. AVD `s24proxy` confirmed via `emulator -list-avds`. Everything before Task 1 is done. If you are a fresh session:
1. Check whether Task 1 finished: `task-1-report.md` exists in this directory AND `package.json` + `jest.setup.ts` + `src/__tests__/smoke.test.tsx` exist at the repo root AND `npm test && npm run typecheck` pass. If all true → append `Task 1: complete` below and go to Task 2. If partially true → the Task 1 steps are idempotent; re-dispatch Task 1 telling the implementer what already exists (it must not re-run create-expo-app if `package.json` is already the Expo app; a leftover `recall-hub-tmp/` folder means the move step never ran).
2. Task 2: Step 1 (env vars) and Step 2 (AVD) are already done — dispatch only Steps 3–6 (boot `s24proxy`, `npx expo start --android` installs Expo Go [approved by user], smoke screenshot).
3. Then Tasks 3 → 23 in order, one implementer per task, per the session rulings above (no reviewer dispatches, no git, controller re-runs `npm test && npm run typecheck` after each report).
Model choice used so far: sonnet for Task 1. Plan: haiku for Tasks 5–9 (pure transcription with tests), sonnet for everything else; opus only if a task gets stuck twice.

## Session 3 (resumed 2026-09-05 after the previous session was terminated by an update)
- Resume audit: reports exist for Tasks 1–15 (2 = BLOCKED); Task 13 report = DONE_WITH_CONCERNS at 03:38 (same minute the ledger was last written). Tasks 18–21 are logged as "dispatched" but have NO reports and NO files on disk (`src/screens/` holds only TabStub; OverlayHost unchanged) — they never ran. Task 16 was waiting on 13. Merged tree at resume: `npm test` 16 suites / 92 tests pass; `npm run typecheck` clean.
- Task 13: complete (controller-verified: 92/92, typecheck clean). Ruling on its concern 1: `@react-navigation/bottom-tabs` is NOT a direct dependency — expo-router vendors it; a type-only import from the module expo-router itself uses is accepted — why: environment fact, type-only, erased at build — cost if wrong: none at runtime. Ruling on concern 2: `StyleSheet.absoluteFillObject` is not present in the installed RN; use `StyleSheet.absoluteFill` wherever a brief writes `absoluteFillObject` — carried into every later dispatch — cost if wrong: none, both spell the same style.
- Task 2: still BLOCKED — `s24proxy` config.ini still says `abi.type=x86_64`; only the x86_64 API 35 image is on disk; no arm64-v8a image yet. Emulator checks stay deferred.
- Tasks 16, 18, 19, 20, 21: (re)dispatched in parallel per the standing parallel ruling (disjoint files; 18–21 told NOT to edit `src/overlays/OverlayHost.tsx`; one batch mount dispatch follows). 17 waits for 16.
- (historical) UPDATE 3 (session 3): COMPLETE + verified: 1, 3–15. IN FLIGHT: 16 (sonnet), 18 (sonnet), 19 (haiku), 20 (sonnet), 21 (haiku) — a `task-N-report.md` means finished; no report = cut off → check the brief's file list on disk and re-dispatch only what is missing. NEXT after all five: one batch dispatch mounts 18–21 in `src/overlays/OverlayHost.tsx` (order: add sheet → recall sheet → VIN help → Toast → SplashStub, with the store selectors each brief's Step 4 names); then Task 17 (needs 15+16); then 22 (visual pass — needs the emulator, so it is blocked with Task 2 until the arm64 `s24proxy` exists) and 23. Gate after each: `npm test && npm run typecheck`.
- Task 18: DONE per report (4/4). Controller check: `npm test -- AddVehicleSheet` 4/4, no straight apostrophes in copy. Full-tree gate deferred until 16/19/20/21 report (their in-flight files are red at this moment). → Task 18: complete (pending the merged-tree gate).
- Task 19: DONE per report (4/4). Controller check: `npm test -- VinHelpScreen` 4/4; punctuation OK (— and · present, no straight apostrophes). → complete (pending the merged-tree gate).
- Task 21: DONE per report (1/1; implementer saw full suite 21 suites / 109 tests green and typecheck clean). Controller check: `npm test -- SplashStub` 1/1. → complete (pending the merged-tree gate).
- Task 20: DONE per report (3/3; implementer saw 21 suites / 109 tests, typecheck clean). Controller check: `npm test -- RecallSheet` 3/3; the em-dash row copy comes from the RECALL_ROWS fixture and renders (test asserts it). → complete (pending the merged-tree gate).
- Batch mount (18–21 Step 4s) dispatched (haiku): edits only `src/overlays/OverlayHost.tsx`; order add → recall → vinhelp → Toast → SplashStub. No test renders OverlayHost (only app/_layout.tsx does), so `useRouter()` inside it is safe under jest. Runs in parallel with Task 16 (disjoint files).
- Task 16: DONE per report (implementer saw 21 suites / 109 tests, typecheck clean). Controller check: `npm test -- GarageScreen` 5/5; no straight apostrophes; garage index route pushes `{ pathname: '/(tabs)/garage/[id]', params: { id } }`. Note carried to Task 22: `.expo/types` not generated this session, so typed-route hrefs are unchecked until `expo start` runs. → complete (pending the merged-tree gate).
- Task 17: dispatched (sonnet), parallel with the batch mount (disjoint: `src/screens/VehicleStatsScreen.tsx`, `app/(tabs)/garage/[id].tsx`, its test vs. `src/overlays/OverlayHost.tsx`).
- Batch mount: DONE (controller-verified: OverlayHost order add → recall → vinhelp → Toast → SplashStub, `router.navigate('/(tabs)/recalls')` on recall details; typecheck clean). Tasks 18, 19, 20, 21: complete (mounted; merged-tree gate runs after Task 17).
- Task 17: DONE per report. Controller check: merged tree `npm test` 22 suites / 113 tests pass; `npm run typecheck` clean; `app/(tabs)/garage/[id].tsx` present; punctuation OK (· ×3, no straight apostrophes). → Task 17: complete.
- MERGED-TREE GATE after 16–21 + mount: 22 suites / 113 tests pass, typecheck clean. Tasks 16, 18, 19, 20, 21: complete (controller-verified on the merged tree).
- (historical) UPDATE 4 (session 3, final): ALL CODE TASKS COMPLETE — 1, 3–21 plus the overlay mount. REMAINING: Task 2 (emulator boot + Expo Go smoke; BLOCKED until the user recreates `s24proxy` on the arm64-v8a API 35 Google Play image — the download was in progress), Task 22 (emulator verification pass + blur tuning + `docs/reference/verification.md`; needs Task 2), Task 23 (on-device pass on the S24 Ultra; needs the phone on Wi-Fi/USB with USB debugging). Deferred emulator checks from Tasks 10, 11, 15, 16, 17 all fold into Task 22. When the AVD exists: re-dispatch Task 2 Steps 3–6 from `task-2-brief.md` (already regenerated for arm64), then 22, then 23. Git is still on hold; no repository exists.

## Session 4 (2026-09-06) — repo published to GitHub; emulator unblocked
- User lifted the **git hold** and asked for the repo to be pushed to <https://github.com/SadeqShryim/GARRS_Mobile> (already created, empty, **public**, default branch `main`). `git init -b main` + first commit + push. `.playwright-mcp/` added to `.gitignore` (MCP scratch output); `node_modules/` was already ignored. 179 files committed.
- PRIVACY RULING (user-approved before the push): the repo is public and the handoff photographs — the user's own pictures of their car — showed a **fully legible licence plate**. Options put to the user were blur / push as-is / exclude the photos / make the repo private; the user chose **blur**. The plate region was destroyed (downsampled to a coarse mosaic, re-expanded, Gaussian-blurred, pasted behind a feathered mask) in the five files that contained it: `crash.png` (228,448)-(334,520); `crash-hero.jpg` (203,400)-(301,466); `tile-1.jpg` (76,88)-(134,124); `tile-2.jpg` (140,0)-(210,18); `tile-3.jpg` (0,63)-(58,105). `tile-4.jpg` and `tile-5.jpg` never showed the plate. Verified visually at 4-5x zoom: unreadable in all five. The unredacted originals were **never committed** — they live only in this session's scratchpad, so they are not recoverable from git history. Deliberate, documented departure from the fidelity mandate; the redacted area is incidental background, no designed element affected.
- Checked and cleared: none of the 11 `docs/reference/*.png` contain photography (all are Garage/Recalls/Profile UI), and no `.dc.html` embeds images as base64 data URIs — so the five asset files were the only copies of the plate.
- **Task 2 UNBLOCKED.** An arm64-v8a AVD now exists: `AvdId=s24ultraProxy`, `abi.type=arm64-v8a`, `hw.device.name=pixel_8`, `image.sysdir.1=system-images\android-35\google_apis_playstore_ps16k\arm64-v8a\`. ⚠️ **The name is `s24ultraProxy`, not `s24proxy`** — the plan, `task-2-brief.md`, and the older notes above all say `s24proxy`, which no longer exists. Substitute the new name at dispatch time. The `_ps16k` (16 KB page size) playstore variant is expected on arm64 API 35, not a problem.
- Controller gate re-run this session on the merged tree: `npm test` **22 suites / 113 tests pass**, `npm run typecheck` **clean** (exit 0). No code changed this session — only image assets, `.gitignore`, `CLAUDE.md`, this ledger, and the new root note.
- Wrote `WHERE-WE-LEFT-OFF.md` at the repo root (the user asked for "a note where we last ended"). Refreshed `CLAUDE.md`: status block, the AVD rename, the lifted git hold, and the "What this repo is right now" section, which still claimed there was no app code.

### RESUME HERE (supersedes the RESUME HERE block above)
CURRENT STATE: unchanged from session 3 on the code side — ALL code tasks (1, 3–21 + overlay mount) complete and controller-verified, 22 suites / 113 tests, typecheck clean. What changed: the repo is now on GitHub, and **the emulator blocker is gone**.
NEXT, in order:
1. **Task 2, Steps 3–6** — boot `s24ultraProxy` (NOT `s24proxy`), `npm run android` installs Expo Go [user-approved], smoke screenshot to `docs/reference/emu-smoke.png`. Dispatch from `task-2-brief.md`, correcting the AVD name. Emulator binaries by absolute path in an existing shell: `"$LOCALAPPDATA/Android/Sdk/emulator/emulator.exe"`, `"$LOCALAPPDATA/Android/Sdk/platform-tools/adb.exe"`.
2. **Task 22** — emulator fidelity pass vs. `docs/reference/*.png`, final blur values in `src/theme/tokens.ts`, write `docs/reference/verification.md`. Absorbs the deferred emulator checks of Tasks 10, 11, 15, 16, 17 and the typed-route href re-check once `expo start` has generated `.expo/types`.
3. **Task 23** — on-device pass on the S24 Ultra, appended to `verification.md`.
Rules still in force: no reviewer dispatches, no Playwright/Chromium download, gate after each task = `npm test && npm run typecheck`. Git is now allowed. Model plan: sonnet for 2/22/23.
